# Wicket connections. Hand-edited; keep the comments.

[general]
note = "kept"   # an unknown key, with a comment

# --- Work machines ---------------------------------------------------------

# The office desktop. Domain login.
[[profiles]]
name   = "work"          # aligned by hand
host   = "192.168.1.20"
user   = "jdoe"          # the SSO account
domain = "CORP"
tags = [
  "office",   # the tower under the desk
  "daily",
]
scale = 140

# The lab box. Somebody once put its password in here.
[[profiles]]
  name = 'lab'
  host = "lab.example.com:3390"
  user = "admin"
  size = "1920x1080"
  fullscreen = true

# Trailing notes stay at the end.
